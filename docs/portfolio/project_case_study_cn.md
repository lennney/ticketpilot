# TicketPilot 项目案例

## 项目背景

TicketPilot 是一个面向中文客服工单的智能分诊与证据溯源回复助手（Copilot）。项目从零开始，采用 OpenSpec 规范驱动开发模式，通过 6 轮 OpenSpec 变更逐步构建完成。

项目定位为 MVP（最小可行产品），核心目标是验证以下技术可行性：

- 中文客服工单的结构化处理（标准化、意图分类、风险评估）
- 基于混合检索（关键词 + 向量）的证据溯源知识库查询
- 可选的证据溯源草稿回复生成
- 人工审核控制台（Human-in-the-Loop）

## 业务问题

在线客服团队每天处理大量非结构化的中文工单。客服人员需要快速理解工单意图、评估风险等级、查阅相关知识库（FAQ、政策条款、历史案例），然后撰写回复。这一流程存在以下痛点：

1. **工单处理无标准化**：工单以自由文本形式到达，缺乏统一结构，无法自动化处理
2. **风险识别依赖个人经验**：退款纠纷、法律风险、隐私泄露等敏感工单依赖客服个人判断
3. **知识查找耗时**：客服需手动在 FAQ、政策文档和历史案例中搜索相关信息
4. **回复质量参差不齐**：缺乏证据溯源的回复容易产生不准确或误导性内容

TicketPilot 旨在通过自动化流水线解决上述问题，同时保留人工审核机制以控制风险。

## 用户场景与工作流

### 典型场景

某电商平台客服收到用户工单："我要退款，订单号是 12345，你们发的货是坏的，我要赔偿！"

### 系统处理流程

```
用户提交工单 → 标准化处理 → 意图分类 → 风险评估 → 知识检索 → 草稿生成(可选) → 人工审核
```

1. **标准化**：清洗文本、提取订单号（12345）和实体信息
2. **分类**：判定为 REFUND（退款）意图
3. **风险评估**：检测到"赔偿"关键词，触发 COMPENSATION_RISK（赔偿风险）标志
4. **知识检索**：根据退款意图 + 赔偿风险，从 FAQ/政策/案例库中检索相关条款和先例
5. **草稿生成**（可选）：基于检索到的证据，生成带引用的回复草稿
6. **人工审核**：客服在审核控制台中查看草稿，可批准、编辑、升级或驳回

## 系统架构

### 六层架构

TicketPilot 采用模块化流水线架构，共 6 层（4 层核心 + 2 层可选）：

| 层级 | 功能 | 强制/可选 |
|------|------|-----------|
| Layer 1: 工单接入 | 文本标准化、实体提取（订单号、产品、金额） | 强制 |
| Layer 2: 意图分类 | 基于规则的 8 分类（退款、换货、投诉、账号问题等） | 强制 |
| Layer 3: 风险评估 | 8 个风险标志（赔偿、法律、隐私、账户安全等） | 强制 |
| Layer 4: 知识检索 | 混合检索（关键词 + 向量）+ RRF 融合 | 强制 |
| Layer 5: 草稿生成 | 基于证据模板的回复生成（无需 LLM） | 可选 |
| Layer 6: 人工审核 | Streamlit 审核控制台，记录审核决策 | 可选 |

### 核心模块

| 模块 | 目录 | 说明 |
|------|------|------|
| 工单接入 | `src/ticketpilot/intake/` | 文本清洗、实体提取（正则匹配中国订单号、金额） |
| 意图分类 | `src/ticketpilot/classification/` | 基于字典关键词的规则分类器，8 个意图类别 |
| 风险评估 | `src/ticketpilot/risk/` | 8 个风险标志，含严重度计算和人工审核触发逻辑 |
| 知识检索 | `src/ticketpilot/retrieval/` | 混合检索引擎：PostgreSQL FTS + pgvector HNSW + RRF |
| 草稿生成 | `src/ticketpilot/drafting/` | 抽象提供者模式 + FakeDraftProvider + 引用验证器 |
| 人工审核 | `src/ticketpilot/review/` | ReviewDecision 审计模型 + ReviewStore JSONL 持久化 |
| 流水线编排 | `src/ticketpilot/pipeline.py` | 4 阶段流水线，每阶段独立 try/except 降级 |

### 技术栈

| 组件 | 技术 |
|------|------|
| 运行环境 | Python 3.12+ (uv 包管理) |
| 数据库 | PostgreSQL 16 + pgvector |
| 关键词检索 | PostgreSQL FTS (simple 配置 + GIN 索引) |
| 向量检索 | pgvector HNSW (m=16, ef_construction=200, ef_search=100, 余弦距离) |
| 排序融合 | RRF (k=60) |
| 数据建模 | Pydantic |
| 审核界面 | Streamlit (MVP 原型) |
| 开发模式 | OpenSpec 规范驱动开发 |
| 容器化 | Docker Compose |

## AI 工作流设计

### 混合检索设计

检索系统采用混合检索策略——关键词检索 + 向量检索——以克服单一检索方式的局限性：

- **关键词检索（PostgreSQL FTS）**：适合精确匹配政策编号（如"7.3.2"）、产品代码等。使用 `simple` 分词配置，搭配 8 个中文业务关键词的 LIKE 回退机制
- **向量检索（pgvector HNSW）**：适合语义相似的查询匹配。MVP 阶段使用 FakeEmbeddingProvider（384 维伪随机向量），仅用于流水线验证
- **RRF 融合**：将两种检索结果按排名融合，k=60

**当前实现约束**：
- 嵌入向量由 FakeEmbeddingProvider 生成（SHA-256 哈希种子确定性伪随机），**无语义含义**，仅用于验证流水线机制
- 知识库为合成种子数据（12 条 FAQ + 12 条政策 + 12 条案例），非真实企业数据
- 查询构建器将意图和风险标志映射为中文业务术语扩展检索查询

### 草稿生成设计

草稿生成采用可选工作流设计，不改变核心流水线的返回值约定：

- **提供者模式**：`AbstractDraftProvider` 抽象接口 + `FakeDraftProvider` 确定性模板实现（无 LLM、无网络、无 API Key）
- **引用验证器**：基于正则的确定性验证，检查引用标注完整性和无引用声明检测
- **安全回退**：无证据时返回安全消息（无确定性承诺），高风险时标记 `must_human_review=True`
- **置信度计算**：基于证据数量和风险标志的计算，无证据时 confidence=0.0

### 流水线完整性保障

- 每阶段均有 try/except 降级处理，流水线永不崩溃
- 检索阶段单独隔离，失败时返回空证据 + `INSUFFICIENT_EVIDENCE` 标志
- 不可变标志处理：`_with_added_risk_flag()` 辅助函数创建新对象而非修改原集合
- 可选阶段不改变核心返回值类型，保持向后兼容

## 人工审核设计

### 审核机制

人工审核是系统的安全核心，而非可选附加功能：

- **审核触发条件**：高风险标志、草稿未受支持声明、证据不足、生成错误都会触发 `must_human_review=True`
- **四种审核操作**：APPROVE（批准）、EDIT（编辑）、ESCALATE（升级）、REJECT（驳回）
- **审计追踪**：每条审核记录包含审核时的完整快照（原始草稿、风险状态、意图、引用摘要、证据使用量）

### 安全约束

- **不自动发送回复**：审核控制台仅记录决策，无任何发送功能。界面明确标注"审核控制台 — 不自动发送回复"
- **审核决策持久化**：采用追加式 JSONL 文件存储，每行一个 `ReviewDecision` 记录
- **无认证机制**：审核员身份为自由文本标签，适用于本地 MVP 演示

## 质量标准与测试纪律

### 质量门禁

项目维护严格的自动化质量门禁脚本 `scripts/run_quality_gate.sh`，包含 5 个阶段：

| 检查项 | 阈值 | 说明 |
|--------|------|------|
| Ruff 静态检查 | 0 错误 | 所有源文件和测试文件 |
| 单元测试 | 全部通过 | 覆盖率 >= 70% |
| 集成测试 | 全部通过，0 跳过 | 跳过计数守卫，DB 不可用时需显式绕过 |
| OpenSpec 验证 | 全部通过 | 验证规范与实现一致性 |
| 密钥扫描 | 0 个 | 扫描 OpenAI 格式密钥 |

### 测试度量（最新）

- **单元测试**：325 个全部通过
- **集成测试**：74 个全部通过，0 跳过
- **代码覆盖率**：80.25%（高于 70% 阈值）
- **8 个黄金用例**：涵盖退费、赔偿、投诉、隐私、账号安全、法律、多问题、模糊工单等场景

### 测试纪律

- 单元测试隔离数据库依赖（使用 mock），始终可运行
- 集成测试连接真实 PostgreSQL + pgvector，验证完整流水线
- 集成测试跳过计数守卫确保隐式跳过被捕获
- `|| true` 历史问题已在审计修复（Stage 04）中全面解决

## 当前演示能力

### 可演示内容

1. **端到端流水线**：从 `RawTicket` JSON 输入到完整 `TicketOutput`（标准化、分类、风险评估、证据检索）
2. **混合检索**：关键词 + 向量检索 + RRF 融合，含完整检索追踪
3. **草稿生成**：基于证据模板的回复草稿（带引用标注）
4. **人工审核控制台**：Streamlit 界面展示完整审核工作流
5. **审核决策持久化**：审核记录写入追加式 JSONL 文件

### 演示启动方式

```bash
# 启动数据库
docker compose up -d

# 运行质量门禁
bash scripts/run_quality_gate.sh

# 启动审核控制台
streamlit run src/ticketpilot/review/console.py
```

## 局限性

### 当前阶段的有限性

- **嵌入向量无语义**：`FakeEmbeddingProvider` 生成伪随机向量，无法验证真正的检索质量
- **种子数据为合成数据**：36 条文档（12 FAQ、12 政策、12 案例），非真实企业数据
- **无真实 LLM**：草稿生成使用模板式 `FakeDraftProvider`，无 OpenAI/Claude 等连接
- **无评估流水线**：未建立自动化检索质量或生成质量评估
- **无持久化追踪**：检索追踪和草稿生成追踪为内存对象，未持久化到数据库
- **无认证/多用户**：审核员身份为自由文本标签，无登录和权限控制
- **无生产部署**：仅提供 Docker Compose 本地开发环境

## 下一步计划

### 短期（向 MVP 就绪）

1. **真实数据包**：替换合成种子数据为真实 FAQ、政策和案例文档
2. **真实嵌入提供者**：接入真实嵌入模型（计划小模型 384 维、高质量模型 768 维两档）
3. **真实 LLM 提供者**：通过抽象接口接入 OpenAI/Claude 等 LLM 提供者
4. **评估流水线**：建立基于黄金问答对的检索和生成质量评估

### 中期（向产品化）

1. **LangGraph 工作流编排**：替代当前线性流水线，支持更复杂的分支和条件逻辑
2. **追踪持久化**：将检索追踪和生成追踪持久化到数据库
3. **认证与多用户审核**：实现登录、角色和权限的审核工作流
4. **数据库后端**：将 JSONL 持久化替换为共享数据库

### 长期（生产就绪）

1. **生产部署**：Docker 生产配置、CI/CD、云部署
2. **真实客服系统集成**：对接工单系统和发送回复的 API
3. **自动发送**：在人工审核通过后实现一键发送（目前仅记录决策）
4. **观测性集成**：接入 Langfuse、Ragas 等可观测平台
